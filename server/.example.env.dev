# Backend and frontend ports respectively
PORT=3001
CLIENT_PORT=3000

# Spotify API keys
# Don't commit these to git
CLIENT_ID=
CLIENT_SECRET=

# The url of your server where callback is called
# Must match exactly what you set in the spotify dev dashboard
REDIRECT_URI=localhost:3001/callback
